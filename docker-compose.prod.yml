services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_INSTANT_APP_ID: ${NEXT_PUBLIC_INSTANT_APP_ID}
        NEXT_PUBLIC_CLSI_URL: ${NEXT_PUBLIC_CLSI_URL}
        NEXT_PUBLIC_ENABLE_AI_CHAT: ${NEXT_PUBLIC_ENABLE_AI_CHAT:-false}
        NEXT_PUBLIC_RAILWAY_ENDPOINT_URL: ${NEXT_PUBLIC_RAILWAY_ENDPOINT_URL:-}
        NEXT_PUBLIC_STRIPE_PRICE_ID: ${NEXT_PUBLIC_STRIPE_PRICE_ID:-}
    environment:
      NODE_ENV: production
      ENABLE_AI_CHAT: ${ENABLE_AI_CHAT:-false}
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY:-}
      GOOGLE_GENERATION_AI_API_KEY: ${GOOGLE_GENERATION_AI_API_KEY:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_PRICE_ID: ${STRIPE_PRICE_ID:-}
      NEXT_PUBLIC_ENABLE_AI_CHAT: ${NEXT_PUBLIC_ENABLE_AI_CHAT:-false}
      NEXT_PUBLIC_STRIPE_PRICE_ID: ${NEXT_PUBLIC_STRIPE_PRICE_ID:-}
    restart: unless-stopped
    depends_on:
      - clsi
    networks:
      - appnet

  clsi:
    build:
      context: ./services/cltsi
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3013
      FRONTEND_URL: ${FRONTEND_URL}
      COMPILE_TIMEOUT: ${COMPILE_TIMEOUT:-60000}
      CACHE_AGE: ${CACHE_AGE:-5400000}
      CACHE_LIMIT: ${CACHE_LIMIT:-2}
      TYPST_ALLOW_NETWORK: ${TYPST_ALLOW_NETWORK:-false}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COMPILE_RATE_LIMIT_ENABLED: ${COMPILE_RATE_LIMIT_ENABLED:-true}
      COMPILE_RATE_LIMIT_USER_HEADER: ${COMPILE_RATE_LIMIT_USER_HEADER:-x-clarity-user-id}
      COMPILE_RATE_LIMIT_COOLDOWN_LIMIT: ${COMPILE_RATE_LIMIT_COOLDOWN_LIMIT:-1}
      COMPILE_RATE_LIMIT_COOLDOWN_WINDOW_SEC: ${COMPILE_RATE_LIMIT_COOLDOWN_WINDOW_SEC:-2}
      COMPILE_RATE_LIMIT_BURST_LIMIT: ${COMPILE_RATE_LIMIT_BURST_LIMIT:-60}
      COMPILE_RATE_LIMIT_BURST_WINDOW_SEC: ${COMPILE_RATE_LIMIT_BURST_WINDOW_SEC:-600}
      COMPILE_RATE_LIMIT_AUTO_LIMIT: ${COMPILE_RATE_LIMIT_AUTO_LIMIT:-20}
      COMPILE_RATE_LIMIT_AUTO_WINDOW_SEC: ${COMPILE_RATE_LIMIT_AUTO_WINDOW_SEC:-60}
      COMPILE_RATE_LIMIT_DAILY_LIMIT: ${COMPILE_RATE_LIMIT_DAILY_LIMIT:-200}
      COMPILE_RATE_LIMIT_DAILY_WINDOW_SEC: ${COMPILE_RATE_LIMIT_DAILY_WINDOW_SEC:-86400}
      UPSTASH_REDIS_REST_URL: ${UPSTASH_REDIS_REST_URL:-}
      UPSTASH_REDIS_REST_TOKEN: ${UPSTASH_REDIS_REST_TOKEN:-}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - clsi_compile:/tmp/clsi/compiles
      - clsi_output:/tmp/clsi/output
    restart: unless-stopped
    networks:
      - appnet

  caddy:
    image: caddy:2.8
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
      - clsi
    restart: unless-stopped
    networks:
      - appnet

networks:
  appnet:

volumes:
  clsi_compile:
  clsi_output:
  caddy_data:
  caddy_config:
