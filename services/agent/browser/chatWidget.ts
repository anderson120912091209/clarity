// Main chat container orchestration placeholder.
export interface IChatWidget {}

// Orchestration layer for chat UI and session coordination.
export class ChatWidget implements IChatWidget {
  public static readonly CONTRIBS: Record<string, never> = {}
}
